# 即時多人協作白板／筆記專題－聊天室交接摘要

## 一、專題背景

- 專題類型：大學資工專題
- 人數：2 人
- 預計開發時間：約 2 個月
- 題目方向：**即時多人協作白板／筆記系統**
- 核心目標：兩個以上使用者進入同一房間後，可以即時看到彼此對白板與筆記內容的操作。
- 現階段優先目標：**先完成本機 Server 版本，不先做雲端部署。**
- Railway：**未來部署階段再處理，不是目前開發重點。**

---

## 二、目前確定的技術

### 前端
- React
- TypeScript
- Konva.js / react-konva

用途：
- 白板 UI
- 矩形、圓形、文字
- 物件選取
- 移動
- 刪除
- 自由畫筆
- 接收即時事件後更新畫面

### 後端
- Node.js
- Express
- Socket.IO

用途：
- 本機 Server
- WebSocket 即時連線
- Room 管理
- 使用者加入 / 離開
- 白板操作廣播
- Server 端 Room State

### 後續技術
- PostgreSQL：之後用於 Snapshot、房間資料、歷史資料保存
- CRDT：之後只考慮用在多人文字協作
- Railway：最後部署階段
- Redis：目前不做，除非未來需要多 Server

---

## 三、目前核心架構

```text
Browser A                    Browser B
React                        React
Konva                        Konva
   │                            │
   │       Socket.IO            │
   └────────────┐  ┌────────────┘
                ▼  ▼
          Node.js Server
             Express
             Socket.IO
                │
                ▼
         Room State in RAM
```

原則：

> Client 負責畫面與操作，Server 負責同步資料。

Server 不負責畫 Canvas。

---

## 四、同步方式

系統不是傳整張畫面，而是傳送「操作」或「物件變更」。

採用：

- Operation-based Synchronization
- Snapshot Synchronization
- Local First / Optimistic Update
- Server RAM 保存即時 Room State

### Operation 範例

```json
{
  "event": "object:update",
  "roomId": "room001",
  "objectId": "rect-001",
  "changes": {
    "x": 300,
    "y": 250
  },
  "version": 3
}
```

### 同步流程

```text
A 操作物件
↓
A 本地立即更新
↓
socket.emit(operation)
↓
Node.js Server
↓
更新 Room State
↓
broadcast
↓
B 收到 operation
↓
更新自己的 Canvas
```

---

## 五、Snapshot 設計

新使用者加入房間時，不可能收到以前已經發生過的 WebSocket Event。

因此：

```text
第一次加入 Room
→ Server 傳完整 Snapshot

加入之後
→ 只接收 Operation
```

也就是：

```text
Snapshot Synchronization
+
Operation Synchronization
```

---

## 六、50ms 同步原則

50ms 是 **Throttle 上限**，不是固定每 50ms 傳整張白板。

正確方式：

```text
使用者沒操作
→ 不傳

正在拖曳
→ 最多每 50ms 傳一次

drag end
→ 立即傳最後位置
```

自由畫筆也採用：

- 多個 points 先累積
- 每約 30～50ms batch 一次
- 不要每個 mousemove 都送

資料庫也不能每 50ms 寫一次。

---

## 七、目前預計 Socket.IO Event

第一版：

```text
room:join

object:create
object:update
object:delete

user:join
user:leave

snapshot
```

第二階段再加入：

```text
cursor:move

draw:start
draw:points
draw:end

object:lock
object:unlock
```

---

## 八、白板 Object 建議格式

```ts
type WhiteboardObject = {
  id: string;
  type: "rect" | "circle" | "text" | "stroke";

  x: number;
  y: number;

  width?: number;
  height?: number;

  rotation?: number;
  text?: string;

  version: number;
};
```

前後端必須共用一致的資料格式。

---

# 九、雙人分工

## A－Frontend / Whiteboard

主要技術：
- React
- TypeScript
- react-konva

可以獨立完成：

- 白板畫面
- 工具列
- 矩形
- 圓形
- 文字
- 自由畫筆
- Object Create
- Object Select
- Object Move
- Object Delete
- Canvas Local State
- 游標 UI
- 前端 Throttle
- 收到 Socket Event 後更新 Canvas

A 的第一階段目標：

> 完全不連 Server，也能完成一個可操作的單機白板。

---

## B－Backend / Realtime

主要技術：
- Node.js
- Express
- Socket.IO

可以獨立完成：

- Socket.IO Connection
- Room Join
- Room Leave
- User Join / Leave
- Object Event Broadcast
- Room State
- Snapshot
- Server Memory State
- Version 檢查
- Object Lock
- Reconnect
- Room Cleanup
- Error Handling

後續：
- PostgreSQL
- Snapshot Persistence
- 歷史紀錄

B 不需要等待前端完成，可以先用簡單 HTML 或 console client 測試 Socket.IO。

---

## 十、共同負責的部分

兩人一開始就要先共同決定：

### 1. Socket Protocol

事件名稱必須固定，例如：

```text
object:create
object:update
object:delete
```

不要出現前端叫：

```text
moveObject
```

後端卻叫：

```text
objectMove
```

### 2. Object 資料格式

必須先統一：

- id
- type
- x
- y
- width
- height
- version
- roomId

### 3. 第一次整合

順序：

```text
object:create
↓
object:update
↓
object:delete
```

不要第一次就一次整合所有功能。

### 4. 後期共同項目

- CRDT
- 系統整合
- 壓力測試
- Demo
- 簡報
- 報告
- Railway 部署

---

# 十一、建議開發順序

```text
1. React + TypeScript
↓
2. Node.js + Express
↓
3. Socket.IO
↓
4. Room
↓
5. react-konva
↓
6. Object Create / Update / Delete
↓
7. Server Room State
↓
8. Snapshot
↓
9. Throttle / Batch
↓
10. Version
↓
11. Reconnect
↓
12. Object Lock / Last Write Wins
↓
13. PostgreSQL
↓
14. 多人文字
↓
15. CRDT
↓
16. Railway 部署
```

---

# 十二、目前第一個 Milestone

現在最重要的目標：

> **同一台電腦開兩個瀏覽器，兩邊進入同一個 Room。A 建立並拖曳一個矩形，B 可以即時看到矩形出現與移動。**

完成這個以前：

- 不做 PostgreSQL
- 不做 Redis
- 不做 CRDT
- 不做 Railway
- 不做完整登入系統
- 不急著做漂亮 UI

---

# 十三、下一步

下一個聊天室如果要直接開始開發，請從：

```text
建立專案結構：

collab-whiteboard/
├─ client/
│  └─ React + TypeScript
└─ server/
   └─ Node.js + Express + Socket.IO
```

開始。

第一個實作項目：

> **先完成 Socket.IO Room，確認兩個 Browser 可以連進同一 Room 並互相收到事件。**

之後再接 react-konva 白板。

---

## 給下一個 ChatGPT 的提示

目前專題還在「本機 MVP」階段。

請不要一開始就把方向帶到：

- Railway
- Redis
- Kubernetes
- 多 Server
- 完整 CRDT
- 過度複雜的分散式架構

除非使用者主動要求。

目前最重要的是：

> **先讓 Local Node.js + Socket.IO + React + react-konva 的多人白板核心同步成功。**
